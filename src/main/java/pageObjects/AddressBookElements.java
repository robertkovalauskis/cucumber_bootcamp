package pageObjects;

import core.BaseCore;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.FindBy;

public class AddressBookElements extends BaseCore {
    public AddressBookElements(WebDriver driver) {
        super(driver);
    }

    @FindBy



}
